# Script 1
cal.sh is a simple calculator that takes 3 input 
- Num1, 
- Num 2, 
- Add or Subtract and either add or subctract base on user input
and it does the calculation for the user  and outputs the final answer

## Use functions to create the add and subtraction operations
## Use case read to take user inputs and operation they wish to perform
## Based on the users option of operation, the case statement executes the operation on the user's input


#Script 2
user.sh gets a user Name and Surname
and uses this to create a user with username -  namesurname@devopsclass.com

